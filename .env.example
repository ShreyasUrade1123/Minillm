# Environment variables — copy to .env.local and fill in values
# Never commit .env.local to version control

VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_NAME=Minillm
VITE_APP_VERSION=0.1.0
